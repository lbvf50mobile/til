# Leetcode: 1185. Day of the Week.

```Ruby
# Leetcode: 1185. Day of the Week.
# https://leetcode.com/problems/day-of-the-week/
# @param {Integer} day
# @param {Integer} month
# @param {Integer} year
# @return {String}
def (day, month, year)
    names = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"]
    check = ->(a,b){
        (0...3).each do |i|
            return -1 if a[i] < b[i]
            return 1 if a[a] > b[i]
        end
        return 0
    }
    leap = ->x{ (0 != x%100 &&  0 == x%4) || (0 == x%100 && 0 == x%400)}
end
```