# Leetcode: 1314. Matrix Block Sum. 


```Ruby
# Leetcode: 1314. Matrix Block Sum.
# https://leetcode.com/problems/matrix-block-sum/
# @param {Integer[][]} mat
# @param {Integer} k
# @return {Integer[][]}
def matrix_block_sum(mat, k)
    h,w = mat.size, mat[0].size
    
    dp = Array.new(h).map{ Array.new(w,0)}
    
    ans = Array.new(h).map{ Array.new(w,0)}
    
    dp[0][0] = mat[0][0]
    # Fill row.
    (1...w).each{|j| dp[0][j] = mat[0][j] + mat[0][j-1]}
    # Fill column.
    (1...h).each{|i| dp[i][0] = mat[i][0] + mat[i-1][0]}
    # Fill the rest.
    (1...h).each do |i|
        (1...w).each do |j|
            dp[i][j] = mat[i][j] + dp[i-1][j] + dp[i][j-1]
        end
    end
    
    p dp
    
    [[12,21,16],[27,45,33],[24,39,28]]
end
```